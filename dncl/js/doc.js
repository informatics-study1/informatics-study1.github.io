const menuButton=document.getElementById('menu-button'),sidebar=document.getElementById('sidebar'),searches=[...document.querySelectorAll('.doc-search')],sections=[...document.querySelectorAll('[data-search-section]')],noResults=document.getElementById('no-results'),tocLinks=[...document.querySelectorAll('.toc a')];
menuButton?.addEventListener('click',()=>{const open=sidebar.classList.toggle('is-open');menuButton.setAttribute('aria-expanded',String(open))});
tocLinks.forEach(link=>link.addEventListener('click',()=>{sidebar.classList.remove('is-open');menuButton?.setAttribute('aria-expanded','false')}));
function filterSections(value){const query=value.trim().toLocaleLowerCase('ja');searches.forEach(input=>{if(input.value!==value)input.value=value});let matches=0;sections.forEach(section=>{const visible=!query||section.textContent.toLocaleLowerCase('ja').includes(query);section.classList.toggle('search-hidden',!visible);if(visible)matches++});noResults.hidden=matches>0}searches.forEach(input=>input.addEventListener('input',event=>filterSections(event.target.value)));
if('IntersectionObserver'in window){const observer=new IntersectionObserver(entries=>{const current=entries.filter(entry=>entry.isIntersecting).at(-1);if(!current)return;tocLinks.forEach(link=>link.classList.toggle('is-active',link.hash===`#${current.target.id}`))},{rootMargin:'-10% 0px -75% 0px'});document.querySelectorAll('main section[id]').forEach(section=>observer.observe(section))}
